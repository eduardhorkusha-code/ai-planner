# AI-Planner — Roadmap до фіналу

> **Візія:** Todoist з AI-серцем — кинув думки голосом/текстом → Claude розклав їх у реалістичний план дня. **Live:** https://ai-planner-swart.vercel.app · **Дедлайн:** 2026-06-06 17:45 (демо 3 хв на телефоні) · **Стабільний коміт:** `cbd4d9c`

---

## Поточний стан

| Фіча | Статус | Файл |
|------|--------|------|
| Маршрутизація `/` → `/capture`, mobile-first контейнер `max-w-md` | ✅ | `app/page.tsx`, `app/layout.tsx` |
| TabBar нижня навігація з активним станом | ✅ | `app/TabBar.tsx` |
| Capture: textarea + loading/error/disabled | ✅ | `app/capture/page.tsx` |
| AI parse `/api/parse` (Haiku, try/catch, markdown-strip) | ✅ | `app/api/parse/route.ts` |
| Інцидент U+2028 закритий (ключ + клієнт-санітайз) | ✅ | `route.ts:15`, `capture/page.tsx:96-99` |
| Inbox: фільтр, «На сьогодні»/«Видалити», бейджі | ✅ | `app/inbox/page.tsx` |
| Today: чекліст, progress, «~Xгод залишилось», warning >480хв | ✅ | `app/today/page.tsx` |
| Store: localStorage CRUD + SSR-guard + try/catch | ✅ | `lib/store.ts`, `lib/types.ts` |
| Голос (Web Speech `uk-UA`, fallback) | ⚠️ | `app/capture/page.tsx:50-92` |
| Demo seed (5 задач) | ⚠️ дублюється при повторі | `app/inbox/page.tsx:10-16,35-39` |
| Реалістичність плану | ⚠️ лише статичний warning >480хв | `app/today/page.tsx:56-58` |
| **Валідація JSON від Haiku** (`Array.isArray` + поля) | ❌ **найвищий ризик демо** | `app/api/parse/route.ts:42` |
| Валідація `deadline` (формат/минула дата) | ❌ | `app/inbox/page.tsx:72` |
| `viewport` meta (`width=device-width`, `viewport-fit`) | ❌ | `app/layout.tsx` |
| Тач-зони ≥44px на кнопках Inbox/Today | ❌ | `inbox/page.tsx:81-86`, `today/page.tsx:59` |
| Sticky CTA «Розібрати з AI» над клавіатурою | ❌ | `app/capture/page.tsx:155-162` |
| Тема/шрифт: світлі CSS-змінні + `Arial` перекривають Geist | ⚠️ дрібний конфлікт | `app/globals.css:3-26` |
| Supabase persistence (`wijxqpoghyaizaupmcmv`) | ❌ не підключено | — |
| Google Auth / будь-який логін | ❌ | — |
| Редагування задачі / Task detail | ❌ | — |
| Week-таб, фільтри, drag-and-drop, перенос невиконаного, експорт у GCal, аналітика | ❌ | — |
| Автотести / lint у пайплайні | ❌ лише `tsc`+`build` | — |

**Stack-факт:** реально `Next.js 16.2.7 (Turbopack) + React 19.2.4` (не Next 14 з брифу). `tsc --noEmit` = 0 помилок, `next build` зелений.

---

## План до 17:45

> Бюджет: ~1 год 45 хв. Сума зусиль P0+P1 ≈ 91 хв серійно, але з паралеллю (Dev ∥ Designer) wall-clock ≈ **P0 ~25 хв + P1 ~30 хв = ~55 хв**, лишається ~50 хв на GATE-деплої + репетицію демо. P2 — лише за залишком. Кожна правка ізольована й не ламає робоче.

### P0 СТАБІЛІЗАЦІЯ — спочатку зелене, тоді розширення (зусилля ~40 хв · wall-clock ~25 хв)

| # | Фіча | Статус | Час | Залежності | Паралель | Агент |
|---|------|--------|-----|------------|----------|-------|
| P0-1 | **Guard JSON від Haiku**: `Array.isArray` + фільтр валідних полів (`title:string`, `priority∈{must,nice}`, `estimateMin:number`, `deadline:string\|null`); підняти `max_tokens` 1024→2048 | ❌→🔧 | 15 | — | ні (центральний ризик, робити першим) | Dev |
| P0-2 | **`viewport` meta** у `layout.tsx`: `width=device-width, initialScale=1, viewportFit=cover, maximumScale=1` | ❌→🔧 | 5 | — | так (з P0-1) | Dev/Designer |
| P0-3 | **TabBar overlap fix**: `main` `pb-20`→`pb-28`/`pb-32` + TabBar `pb-[env(safe-area-inset-bottom)]` — щоб остання картка не ховалась | ❌→🔧 | 5 | — | так (з P0-1) | Designer |
| P0-4 | **Capture висота**: прибрати `min-h-screen` (дубль padding), textarea `min-h-[40vh]` замість `h-full` — щоб поле/кнопка не схлопувались під клавіатурою | ❌→🔧 | 8 | P0-3 (узгодити padding) | ні (конфлікт із P0-3 по layout) | Dev |
| P0-5 | **Підтвердити core-флоу на РЕАЛЬНОМУ телефоні** (Capture→AI→Inbox→Today, короткий dump) + **зафіксувати стабільний коміт** | ⚠️→✅ | 7 | P0-1..4 | ні (gate перед P1) | QA→Release Manager |

### P1 MUST для сильного демо (зусилля ~51 хв · wall-clock ~30 хв)

| # | Фіча | Статус | Час | Залежності | Паралель | Агент |
|---|------|--------|-----|------------|----------|-------|
| P1-1 | **Тач-зони ≥44px**: Inbox «На сьогодні»/«Видалити» `py-3 min-h-[44px]`, `aria-label` на 🗑; Today чекбокс `w-6`→`w-7` + `active:scale-95` | ❌→🔧 | 8 | P0-зелене | так (з P1-2) | Designer |
| P1-2 | **Sticky CTA «Розібрати з AI»** над TabBar (`sticky bottom-20 bg-gray-950`) + прибрати `autoFocus` — головна кнопка завжди видима | ❌→🔧 | 10 | P0-4 (Capture layout) | ні (той самий файл, що P0-4) | Dev |
| P1-3 | **Demo seed без дублів**: перевіряти наявність seed-задач / replace замість append; одна чиста кнопка «🎭 Демо» | ⚠️→🔧 | 6 | — | так (з P1-1) | Dev |
| P1-4 | **Реалістичність плану з цифрою**: `⚠️ ~{hoursLeft}год запланованих — більше за робочий день (8 год)` — продає AI-родзинку | ⚠️→🔧 | 6 | — | так (з P1-1) | Dev |
| P1-5 | **Loading-спінер** на Capture: `animate-spin` + `disabled` textarea/мікрофон під час запиту — «воно думає», не «зависло» | ❌→🔧 | 6 | P1-2 | ні (той самий файл) | Dev |
| P1-6 | **Надійність голосу**: лишити як бонус, але індикатор «● Слухаю» зробити видимим (поверх textarea/біля мікрофона), fallback-текст у Chrome; **на сцені не головний шлях** | ⚠️→🔧 | 7 | — | так (з P1-1) | Dev/Designer |
| P1-7 | **Порожні стани + формат deadline**: `formatDeadline` («Сьогодні»/«Завтра»/`6 черв`), щільніші картки (`p-4`→`p-3`), `active:` фідбек на кнопках | ❌→🔧 | 8 | P1-1 | ні (той самий файл Inbox) | Designer |

### P2 ADVANCED — лише якщо лишиться час (НЕ чіпати перед демо)

| # | Фіча | Статус | Час | Залежності | Паралель | Агент |
|---|------|--------|-----|------------|----------|-------|
| P2-1 | **Перенос невиконаного на завтра** (найдешевша advanced-цінність, без БД) — кнопка «→ завтра» для `today` що не `done` | ❌ | 20 | P0+P1 зелене | так (з P2-2) | Dev |
| P2-2 | **Святкування 100%** у Today: зелений progress + «🎉 Все зроблено!» + потовщити бар `h-2`→`h-2.5` — кульмінація демо | ❌ | 10 | — | так (з P2-1) | Designer |
| P2-2b | **Тема/шрифт fix** у `globals.css`: прибрати невикористаний світлий блок + `body{font-family:Arial}`, лишити Geist — щоб під камерою не виглядало «дешево» | ⚠️ | 5 | — | так (з P2-1/P2-2) | Designer |
| P2-3 | **Supabase persistence** (`wijxqpoghyaizaupmcmv`): `@supabase/supabase-js`, `lib/supabase`, таблиця `tasks`, env у Vercel; почати з anon read/write **без RLS-блокера** | ❌ | 40 | P0+P1 зелене, env у Vercel | ні (інфра-залежність, ризик зламати деплой) | Infra→Dev |
| P2-4 | **Google Auth** (Supabase OAuth) — лише ПІСЛЯ P2-3 | ❌ | 35 | P2-3 | ні | Dev |
| P2-5 | Week-таб / фільтри / Task detail / drag-and-drop / експорт у GCal / аналітика | ❌ | — | P2-3/4 | — | Dev |

> **Чесний ризик:** P2-3/P2-4 (Supabase + Auth) **не влазять у бюджет до 17:45 разом з демо-репетицією**. Підключення БД + міграція стану + env на Vercel + тест = легко 60-90 хв і реальний шанс зламати зелений деплой. **Рекомендація: НЕ починати Supabase до фіналу.** Якщо журі питає про БД — показати як «наступний крок», архітектура під це готова (store.ts ізолює доступ до даних). P2-1 + P2-2 — безпечні дешеві wins без БД, якщо лишиться 30 хв.

---

## Карта паралельності (3 хвилі)

```
ХВИЛЯ 1 — СТАБІЛІЗАЦІЯ (паралельно, wall-clock ~25 хв)
  ├─ Dev:      P0-1 Guard JSON (КРИТИЧНО, найперше) + max_tokens
  ├─ Designer: P0-2 viewport  ∥  P0-3 TabBar overlap
  └─ (P0-4 Capture висота — ПІСЛЯ P0-3, бо спільний layout)
         │
         ▼  GATE: P0-5 тест на телефоні + стабільний коміт (Release Manager) — НЕ йти далі поки не зелене
         │
ХВИЛЯ 2 — СИЛЬНЕ ДЕМО (паралельно, wall-clock ~30 хв)
  ├─ Dev:      P1-2 sticky CTA + autoFocus  →  P1-5 спінер (той самий файл, послідовно)
  ├─ Dev #2:   P1-3 demo seed  ∥  P1-4 реалістичність (різні файли — паралельно)
  ├─ Designer: P1-1 тач-зони ∥ P1-6 індикатор голосу  →  P1-7 формат/щільність Inbox
         │
         ▼  GATE: QA повний прогін флоу + Release Manager: deploy + canary
         │
ХВИЛЯ 3 — ADVANCED (тільки якщо лишилось >30 хв, паралельно)
  ├─ Dev:      P2-1 перенос на завтра
  └─ Designer: P2-2 святкування 100%  ∥  P2-2b тема/шрифт fix
  (Supabase/Auth — ЗАМОРОЖЕНО до фіналу)
```

**Послідовні залежності (не паралелити):** P0-4↔P0-3 (layout), P1-2↔P1-5 (один файл Capture), P1-1↔P1-7 (один файл Inbox). **Кожен GATE — обов'язковий деплой+тест перед наступною хвилею.**

---

## Демо-сценарій (3 хв, live на телефоні)

1. **(0:00–0:20) Контекст.** «AI-планер дня: кидаєш думки — Claude робить реалістичний план. Mobile-first, задеплоєно на Vercel.» Показати URL на екрані телефону.
2. **(0:20–1:10) Capture → AI.** Відкрити `/capture`. Тапнути поле (НЕ покладатись на autoFocus). Ввести **короткий** dump (3-4 задачі, наприклад: «зробити презентацію, подзвонити клієнту о 15, купити каву, відповісти на 3 листи»). Тап sticky-кнопки «Розібрати з AI» → видно спінер «Аналізую...» → перехід в Inbox. *(Якщо Chrome — можна 5с показати голос «● Слухаю», але це БОНУС, не основа.)*
3. **(1:10–2:00) Inbox.** Показати розпарсені задачі: priority-бейджі must/nice, estimate, людяний deadline («Сьогодні»). Тап «📌 На сьогодні» на 2-3 must-задачах.
4. **(2:00–2:40) Today — AI-розум.** Чекліст, must-першими, progress bar, «⏱ ~Xгод залишилось». Якщо сума >8 год — показати **конкретний warning з цифрою** («~11 год — більше за робочий день»). Тапнути 1-2 «виконано» → progress росте. *(Якщо встигли P2-2 — фінішний «🎉 Все зроблено!».)*
5. **(2:40–3:00) Закриття.** «Працює реально, не мокап. AI парсить природну мову в реалістичний план. Далі — персистентність (Supabase) і логін.»

**Підстраховка:** перед сценою заздалегідь натиснути «🎭 Демо» в Inbox як запасний шлях, якщо мережа/AI підведе. Тримати телефон у Chrome.

---

## Definition of Done для фіналу

- [ ] `npx tsc --noEmit` = 0 помилок
- [ ] `next build` зелений
- [ ] P0-1 guard JSON: невалідна відповідь Haiku → не падає, фільтрує биті задачі (перевірити довгим dump'ом)
- [ ] Core-флоу Capture→AI→Inbox→Today пройдено **на реальному телефоні** (не лише в десктоп-браузері)
- [ ] Остання картка Inbox/Today доступна (не під TabBar), кнопки ≥44px тапаються з першого разу
- [ ] CTA «Розібрати з AI» видима при відкритій клавіатурі
- [ ] `viewport` meta присутній, сторінка не зумиться довільно на iPhone
- [ ] Demo seed не дублює список при повторному тапі
- [ ] Warning реалістичності показує конкретну цифру годин
- [ ] Деплой на Vercel оновлено (`npx vercel --prod --yes`), public URL відкривається
- [ ] Стабільний коміт у `main`, дерево чисте
- [ ] Демо-сценарій прорепетировано 1 раз end-to-end з телефону
- [ ] Запасний шлях (🎭 Демо seed) перевірено на випадок падіння мережі/AI

---

## Анти-патерни (НЕ робити)

- ❌ **НЕ починати Supabase/Auth до фіналу** — висока ймовірність зламати зелений деплой, бюджету не вистачає. Архітектура готова, показати як «наступний крок».
- ❌ **НЕ розширювати на нестабільному коміті** — спочатку P0 зелене + зафіксований коміт, тоді P1.
- ❌ **НЕ показувати голос як головний шлях** на сцені — `uk-UA` Web Speech крихкий на iOS Safari. Текст — основа, голос — бонус у Chrome.
- ❌ **НЕ вводити довгий dump на демо** — ризик 1024/2048 token cap обрізати JSON. Короткий ввід 3-4 задачі.
- ❌ **НЕ паралелити правки в одному файлі** (Capture: P0-4/P1-2/P1-5; Inbox: P1-1/P1-7) — конфлікти.
- ❌ **НЕ видаляти існуючу логіку** — additive only, мерджити, не зрізати робочі фікси.
- ❌ **НЕ пропускати GATE-тест перед кожною хвилею** — деплой рано і часто, не ламати робоче перед демо.
- ❌ **НЕ деплоїти без `tsc --noEmit` + `next build`** локально.

# BACKLOG — AI Planner

Адмін-панель: https://ai-planner-swart.vercel.app/admin
Монобанка (преміум-заглушка): https://send.monobank.ua/jar/iBD5d6xnP

---

## 🔥 Пріоритетний беклог — Хвиля 4 (post-auth UX, одразу після Хвилі 3)

1. **Нижнє меню (TabBar) лише після логіну / в застосунку.** Ховати на `/`, `/funnel`, `/login`, `/auth/*`; показувати на `/capture`, `/inbox`, `/today`. *(швидкий fix — користувач помітив, що меню видно на лендінгу)*
2. **Після логіну: імʼя користувача + кнопка «Вийти» (logout).** Header/профіль з даними Google-акаунта. Logout — обовʼязково.
3. **Преміум-оффер (заглушка).** Банер/кнопка → monobank jar, копі: «ранній доступ до нових фіч».
4. **Сторінка профілю з аналітикою тасків.** Імʼя, преміум-CTA, logout + статистика: виконано/всього, за пріоритетом, % завершення, стрік.
5. **Кожен юзер бачить лише свої нотатки.** Demo-safe версія: namespace `localStorage` за `user.id` коли залогінений (гість — спільний ключ). *(Повна версія — таблиця `tasks` у Supabase з RLS per `user_id` — у великому беклозі.)*
6. **2 мови: UK / EN.** Перемикач + словник основних рядків.

> Реалістично під дедлайн 17:45: робимо найдешевше+найвидиміше першим (1→2→3→4), далі 5, i18n (6) — за залишком. Усе additive, core не чіпаємо.

## 📋 Беклог (після демо / якщо лишиться час)

1. **Фото/документи → AI → задачі.** Мультимодальний інпут: прикріпити фото або документ, Claude vision розпізнає й створює таски.
2. **Google Calendar.** Задача з конкретною датою+часом → подія в календарі (потребує OAuth Calendar scope + Calendar API — не «легко», тому беклог).
3. **Welcome email** новому користувачу після реєстрації.
4. **Per-user tasks у Supabase (повна версія).** Міграція `store.ts` на Supabase з RLS — крос-девайс, секьюрно.
5. **Адмінка: список/кількість юзерів** (auth.users) на додачу до воронки.

## ✅ Зроблено

- **Хвиля 1** — стабілізація: JSON-guard від падіння, mobile P0/P1 (viewport, safe-area, sticky CTA, 44px тач-зони, реалістичність плану з цифрою).
- **Хвиля 2** — лендінг + воронка питань (нащо/хто) + демо-режим (preview без логіну) + Google-логін (best-effort) + адмінка з аналітикою воронки (Supabase).
- **Хвиля 3** — iOS-нативний дизайн (SF-шрифт, iOS-палітра, SVG-іконки, blur TabBar) *(будується)*.
